package LeetCode;

public class LongestCommonPrefix {
  /*  public String longestCommonPrefix(String[] strs) {

        if(strs.length==0){
            return "";
        }
        String prefix = strs[0];//set first index as prefix and go on removing chars
        for(int i=1;i<strs.length;i++){
            while(strs[i].indexOf(prefix)!=0){//match max  char until its 0
                //op flow
                //op fl
                prefix = prefix.substring(0, prefix.length()-1);
            }
        }
        return prefix;
    }*/
}
